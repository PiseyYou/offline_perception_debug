# 草坪噪点过滤优化方案 - 最终版本

## 实施日期
2026-04-08

## 问题分析

**观察到的现象**：
- 中间点云视图存在成片的草坪噪点（黄色/棕色）
- Speckle 参数调整过于激进（150/6）但效果不佳
- 噪点是连续的草坪区域，不是孤立点

**根本原因**：
- 草坪区域的深度值本身是连续的，通过了 speckle 检查
- 问题不在视差计算阶段，而在点云生成阶段
- 需要基于深度、位置、高度等特征进行过滤

## 实施的优化策略

### 1. 恢复 Speckle 参数到温和水平 ✅

**原因**：激进的 speckle 参数（150/6）对连续草坪区域无效，且可能误删有效点云

**修改内容**：
```cpp
// 主匹配器
SpeckleWindowSize: 150 → 100（温和水平）
SpeckleRange: 6 → 8（温和水平）

// 半分辨率顶部
SpeckleWindowSize: 120 → 100
SpeckleRange: 2（保持）

// 半分辨率底部
SpeckleWindowSize: 100 → 80
SpeckleRange: 8 → 10
```

**位置**：
- `stereo_block_matcher_init_6m_adaptive()` line 133-143
- `half_top_stereo_block_matcher_init_6m_adaptive()` line 153-203
- `half_bottom_stereo_block_matcher_init_6m_adaptive()` line 205-255

### 2. 策略1：基于深度范围的草坪过滤 ✅

**原理**：夜间草坪噪点通常在近距离（< 2.5m）且位于图像下半部分

**实现位置**：`stereo_process_pci_depth_rgb_seg_det_fusion()` line 819-827

**代码**：
```cpp
// 策略1：草坪区域深度范围过滤（过滤近距离草坪噪点）
if (pc_rgbl.label >= 1 && pc_rgbl.label <= 3)
{
    // 过滤近距离草坪（< 2.5m）且位于图像下半部分
    if (d < 2.5f && y > safe_rows / 2)
    {
        continue;
    }
}
```

**关键参数**：
- 深度阈值：2.5m（可调整范围：1.5-3.0m）
- 图像位置：下半部分（y > safe_rows / 2）

**优点**：
- 计算成本极低（仅比较操作）
- 针对性强，只过滤近距离下半部分的草坪
- 不影响远距离草坪和障碍物

### 3. 策略2：基于 Y 坐标的高度过滤 ✅

**原理**：点云的 Y 坐标代表高度，草坪噪点通常在地面附近（Y > -0.2）

**实现位置**：`stereo_process_pci_depth_rgb_seg_det_fusion()` line 829-837

**代码**：
```cpp
// 策略2：草坪区域高度过滤（基于Y坐标）
if (pc_rgbl.label >= 1 && pc_rgbl.label <= 3)
{
    // 过滤地面附近的草坪点（Y > -0.2 表示接近或高于地面）
    if (pc_rgbl.y > -0.2f)
    {
        continue;
    }
}
```

**关键参数**：
- 高度阈值：-0.2m（可调整范围：-0.1 到 -0.3m）
- Y > -0.2 表示点在地面附近或高于地面

**优点**：
- 基于3D空间位置，更准确
- 过滤地面附近的草坪噪点
- 保留低于地面的有效点云

### 4. 保留邻域一致性检查

**位置**：line 839-856

**功能**：作为第三道防线，过滤孤立的草坪噪点

## 过滤策略的层次结构

```
粗采样阶段（step 4，每4个像素）：
  ↓
1. 标签验证（label 1-200）
  ↓
2. 策略1：深度范围过滤（< 2.5m 且下半部分）
  ↓
3. 策略2：高度过滤（Y > -0.2m）
  ↓
4. 邻域一致性检查（至少3个有效邻居）
  ↓
5. 障碍物深度一致性过滤
  ↓
添加到点云
```

## 参数调优建议

### 策略1 - 深度范围过滤

**当前配置**：
```cpp
if (d < 2.5f && y > safe_rows / 2)
```

**调整方向**：
- **更激进**（过滤更多草坪）：
  ```cpp
  if (d < 3.0f && y > safe_rows / 3)  // 扩大范围
  ```
- **更保守**（保留更多点云）：
  ```cpp
  if (d < 2.0f && y > safe_rows * 2 / 3)  // 缩小范围
  ```

### 策略2 - 高度过滤

**当前配置**：
```cpp
if (pc_rgbl.y > -0.2f)
```

**调整方向**：
- **更激进**（过滤更多）：
  ```cpp
  if (pc_rgbl.y > -0.3f)  // 过滤更低的点
  ```
- **更保守**（保留更多）：
  ```cpp
  if (pc_rgbl.y > -0.1f)  // 只过滤非常接近地面的点
  ```

### 邻域一致性检查

**当前配置**：
```cpp
if (valid_neighbors < 3)
```

**调整方向**：
- **更严格**：`< 4` 或 `< 5`
- **更宽松**：`< 2` 或 `< 1`

## 性能影响评估

### 策略1 - 深度范围过滤
- **计算成本**：极低（2次比较操作）
- **性能影响**：< 0.1ms/帧

### 策略2 - 高度过滤
- **计算成本**：极低（1次比较操作）
- **性能影响**：< 0.1ms/帧

### 总体性能
- **总影响**：< 0.5ms/帧
- **优点**：计算成本远低于 speckle 参数调整

## 预期效果

### 策略1（深度范围过滤）
- 过滤近距离下半部分的草坪噪点
- 预计减少 60-70% 的草坪噪点

### 策略2（高度过滤）
- 过滤地面附近的草坪点
- 预计额外减少 20-30% 的草坪噪点

### 组合效果
- 总体减少 80-90% 的草坪噪点
- 保持障碍物点云完整
- 保持远距离草坪点云（如需要）

## 验证方法

### 测试命令
```bash
# 编译
./build.sh

# 运行 Mode 7
./build/offline_perception_debug_432_sob --mode 7 --input <night_image_path>
```

### 评估指标
1. **噪点数量**：中间点云视图的草坪噪点是否显著减少
2. **有效点云**：障碍物点云是否保持完整
3. **点云密度**：整体点云数量是否合理
4. **处理性能**：帧处理时间增加 < 1ms

## 代码修改位置总结

| 修改内容 | 文件 | 行号 | 说明 |
|---------|------|------|------|
| 恢复主匹配器 speckle | stereo_multi_match.cpp | 133-143 | 100/8 温和水平 |
| 恢复半分辨率顶部 speckle | stereo_multi_match.cpp | 153-203 | 100/2 温和水平 |
| 恢复半分辨率底部 speckle | stereo_multi_match.cpp | 205-255 | 80/10 温和水平 |
| 策略1：深度范围过滤 | stereo_multi_match.cpp | 819-827 | < 2.5m 且下半部分 |
| 策略2：高度过滤 | stereo_multi_match.cpp | 829-837 | Y > -0.2m |

## 编译状态
✅ 编译成功
- 所有目标构建完成
- 无编译错误
- 可执行文件：`./build/offline_perception_debug_432_sob`

## 与之前方案的对比

| 方案 | Speckle 参数 | 过滤策略 | 效果 | 性能影响 |
|------|-------------|---------|------|---------|
| 激进方案 | 150/6 | 仅邻域检查 | 不佳 | 中等 |
| **当前方案** | **100/8** | **深度+高度+邻域** | **预期良好** | **极低** |

## 下一步

1. 运行测试，观察效果
2. 如果噪点仍然存在，调整参数：
   - 降低深度阈值（2.5m → 2.0m）
   - 提高高度阈值（-0.2m → -0.15m）
3. 如果过度过滤，放宽参数：
   - 提高深度阈值（2.5m → 3.0m）
   - 降低高度阈值（-0.2m → -0.3m）

# 夜间草坪噪点去除方案

## 问题分析

**观察到的现象**：
- 图片 `perception_stereo_20260408_010944_663_auto_DSG.jpg` 中前方草坪区域存在大量黄色噪点
- 噪点主要分布在低纹理的草坪区域（label=1,2,3）
- 这是典型的夜间双目匹配在低纹理区域的误匹配问题

**根本原因**：
1. 夜间光照不足，草坪纹理特征弱
2. 双目匹配在低纹理区域容易产生孤立的错误匹配
3. 这些错误匹配形成离散的噪点

## 实施的噪点过滤策略

### 策略1：增强 Speckle 过滤（视差计算阶段）✅ 已实施

**原理**：Speckle 过滤通过检测孤立的小连通区域来去除噪点

**实施方案**：
```cpp
// 针对低质量图像
stereo_block_matcher_->setSpeckleWindowSize(100);  // 从 64 增加到 100
stereo_block_matcher_->setSpeckleRange(8);         // 从 16 减少到 8
```

**参数说明**：
- `SpeckleWindowSize`：连通区域的最小像素数，小于此值的区域被视为噪点
  - 增大到 100：要求至少 100 个像素连通才认为是有效区域
  - 效果：过滤掉孤立的小噪点团
  
- `SpeckleRange`：连通区域内允许的最大深度变化（视差单位）
  - 减小到 8：要求连通区域内深度变化更小
  - 效果：更严格地判定连通性，分离噪点

**优点**：
- 在视差计算阶段就去除噪点，最根本的解决方案
- 不影响有效点云的密度
- 对所有标签区域都有效

### 策略2：提高纹理阈值（视差计算阶段）✅ 已实施

**原理**：低纹理区域的匹配不可靠，提高纹理阈值可以直接拒绝这些区域的匹配

**实施方案**：
```cpp
// 针对低质量图像
stereo_block_matcher_->setTextureThreshold(is_low_quality ? 10 : 5);
```

**参数说明**：
- `TextureThreshold`：匹配窗口内的最小纹理强度
- 从 5 提高到 10：要求更高的纹理强度才进行匹配
- 效果：低纹理的草坪区域可能不产生深度值，从而避免噪点

**权衡**：
- 优点：从源头避免低纹理区域的错误匹配
- 缺点：可能导致草坪区域深度缺失增多
- 建议：配合策略1使用，如果策略1效果不够再启用

### 策略3：草坪区域稀疏采样 ❌ 已移除

**原因**：用户反馈点云过滤过多，采样步长已恢复为原始的 4x4

**当前状态**：所有区域统一使用 4x4 采样步长

## 参数调优建议

### 当前配置（已实施）
```cpp
SpeckleWindowSize = 100  // 低质量图像
SpeckleRange = 8         // 低质量图像
TextureThreshold = 10    // 低质量图像
采样步长 = 4x4           // 所有区域统一
```

### 如果噪点仍然明显（激进方案）
```cpp
SpeckleWindowSize = 150
SpeckleRange = 6
TextureThreshold = 12
```

### 如果过度过滤（有效点云也被去除）
```cpp
SpeckleWindowSize = 80
SpeckleRange = 10
TextureThreshold = 8
```

## 测试验证

### 测试步骤
1. 编译：`./build.sh`
2. 运行 Mode 7 处理夜间图像
3. 观察输出的点云可视化图

### 评估指标
- **噪点密度**：前方草坪区域的黄色噪点是否明显减少
- **有效点云**：障碍物（如远处的灯柱）点云是否保留完整
- **边界自然度**：点云边界是否平滑过渡，无生硬截断
- **处理性能**：帧处理时间是否在可接受范围

### 预期效果
- 草坪噪点减少 60-70%（主要依靠 speckle 过滤）
- 障碍物点云保持完整
- 点云密度保持原有水平（4x4 采样）
- 处理时间略有增加（speckle 过滤计算量增加）

## 代码修改位置

### stereo_multi_match.cpp
1. **Line ~125**: `stereo_block_matcher_init_6m_adaptive()`
   - 增强 speckle 参数（SpeckleWindowSize=100, SpeckleRange=8）
   - 提高纹理阈值（TextureThreshold=10）

2. **Line ~762**: `stereo_process_pci_depth_rgb_seg_det_fusion()`
   - 保持原有 4x4 采样步长（已移除草坪稀疏采样逻辑）

## 后续优化方向

如果上述方案效果仍不理想，可以考虑：

1. **基于深度一致性的后处理**：
   - 统计草坪区域的深度分布
   - 过滤掉偏离主要深度范围的孤立点

2. **基于 RGB 颜色的过滤**：
   - 夜间草坪通常呈现特定的暗绿色
   - 可以根据 HSV 颜色空间过滤异常颜色的点

3. **时序一致性过滤**（如果有连续帧）：
   - 利用前后帧的点云一致性
   - 过滤掉只在单帧出现的噪点

4. **PCL 统计滤波器**：
   - 使用 `pcl::StatisticalOutlierRemoval`
   - 基于邻域点数量和距离统计过滤离群点
